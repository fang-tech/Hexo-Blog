---
title: 69.x-的平方根
date: 2026-03-12T23:13:01+08:00
updated: 2026-03-12T23:13:01+08:00
layout: page
permalink: /algorithm/69.x-的平方根/
categories:
  - 算法
  - binary_search
tags:
  - LeetCode
---

> 二分查找, 注意不能计算mid^2会导致越界, left在开区间的情况下应该从0开始

<!-- more -->

```java
class Solution {
    /**
     * 二分查找, 注意不能计算mid^2会导致越界, left在开区间的情况下应该从0开始
     */
    public int mySqrt(int x) {
        int left = 0, right = (x == Integer.MAX_VALUE ? x / 2 : x + 1);
        while (left + 1 < right) {
            int mid  = left + (right - left) / 2;
            if (mid <= x / mid) {
                left = mid;
            } else {
                right = mid;
            }
        }
        return left;
    }
}
```
